# AI session search implementation plan

Goal: Keep PostgreSQL authoritative; add FTS, reproducible chunks, asynchronous Gemini Embedding 2 / 1536 jobs and pgvector hybrid retrieval without changing raw ingestion guarantees.

Scope: User authorized implementation in the current checkout. Preserve unrelated GitHub changes. No shared DB writes, deployment, cloud credentials, or live backfill in this change. Test only local project_management_test. Browser verification remains opt-in.

- [x] Add core forward migration: FTS GIN, derived search documents, embedding profiles, deduplicated jobs/cache, document references and source revision checkpoints. Keep pgvector activation in explicit optional SQL because current PostgreSQL servers lack extension files; do not make app startup migrations fail.
- [x] Add deterministic chunking and search policy tests: paragraph/code boundaries, original offsets, log suppression with important errors retained, no usage/system embedding. Session/topic extractive summaries retain evidence references and are explicitly labelled extractive.
- [x] Add provider contract and Google Vertex implementation with ADC. Verify actual SDK routing and retrieval instructions. Validate dimensionality, finite nonzero vectors, timeout and typed retryable errors; never log request content/credentials.
- [x] Add resumable session revision indexing and durable embedding jobs. Claim using SKIP LOCKED, lease token, bounded attempts/backoff, partial successes, content hash reuse and manual retries. API calls occur outside DB transactions.
- [x] Add optional pgvector vector table and 1536 cosine HNSW; isolate profile spaces. Hybrid search uses RRF, metadata filters, summary candidate routing with global lexical recall, citations and bounded context. Preserve legacy literal mode/pagination.
- [x] Add explicit inspect/backfill/worker/retry CLI commands; pending jobs and indexed revisions are durable checkpoints. Add model-independent retrieval evaluation metrics.
- [x] Integrate search modes into existing authenticated endpoint/UI, with explicit lexical fallback when vectors/API are unavailable.
- [x] Run unit and rollback-only PostgreSQL integration tests, Prisma validation, pnpm typecheck, pnpm build and diff checks; document live/runtime limits separately.

Design rationale: live read-only inspection found 591 sessions, 35,059 messages, 18,913,318 stored characters, 25,880,880 body bytes and 43,827,200 bytes including message indexes/TOAST. Usage tokens are repeat consumption, not storage volume. No Elasticsearch/OpenSearch. Current collectors exclude raw tool/system/reasoning data; preserve this boundary. Future approved tool collectors can use the same policy without broadening ingestion now.

Verification: 36 focused AI tests PASS, including rollback-only PostgreSQL/pgvector integration and real Google SDK wire contract without network. Prisma validate, typecheck, build and diff check PASS. Production migration, pgvector installation, Vertex IAM/API call, live backfill, scheduled worker and browser are NOT VERIFIED / not performed. Review findings (compose env and profile-aware active job counts) fixed and re-reviewed.
