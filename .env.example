# 로컬 개발용. 사용자 이름과 비밀번호는 각자 환경에 맞춘다.
DATABASE_URL="postgresql://USER@localhost:5432/project_management"
TEST_DATABASE_URL="postgresql://USER@localhost:5432/project_management_test"
# 소유자 최초 등록용 기존 비밀번호: node scripts/hash-password.mjs 결과
APP_PASSWORD_HASH=""
# 소유자 최초 등록 전 서명 세션용: openssl rand -hex 32 결과
SESSION_SECRET=""
# 개발·운영 모두 로그인 필요. 소유자 등록 후 공통 비밀번호는 사용할 수 없다.

# pnpm dev: private SSH tunnel to the shared project database. No server password here.
SHARED_DB_SSH_HOST=your-server.example.com
SHARED_DB_SSH_USER=your-user
SHARED_DB_SSH_PORT=61185
SHARED_DB_SSH_KEY=~/.ssh/your-personal-key
SHARED_DB_REMOTE_PORT=15432
SHARED_DB_LOCAL_PORT=15435

# OCI Object Storage 연결 메타데이터. 개인키/액세스 키를 여기에 복사하지 않는다.
# 로컬은 기존 ~/.oci/config, 운영 컨테이너는 버킷으로 제한한 instance principal 사용.
OCI_STORAGE_REGION=
OCI_STORAGE_NAMESPACE=
OCI_STORAGE_BUCKET=
OCI_STORAGE_AUTH=config_file

# AI search: Vertex AI ADC/workload identity (never store service-account key contents here).
EMBEDDING_PROVIDER=google
EMBEDDING_MODEL=gemini-embedding-2
EMBEDDING_DIMENSIONS=1536
EMBEDDING_VERSION=1
GOOGLE_CLOUD_PROJECT=
GOOGLE_CLOUD_LOCATION=us
